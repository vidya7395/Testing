const enum SeatChoice {
    Window =10,
    Aisle =22,
    Middle,
    EmergencyExit,
    Wing,
}

const hcSeat = SeatChoice.Window;

enum Varinat {
    dark = 0,
    light,
    default
}

const iconId = Varinat.light;

console.log(iconId);

export {}
